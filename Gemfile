source 'https://rubygems.org' 


gem 'rspec'		# for unit testing
gem 'aruba'		# for integration testing the command line interface, works with cucumber
gem 'rake'		# tasks to run tests (mainly fo travis ci)
gem 'mongo'		# to store the passwords (haven't really used it before, thought it would be nice to try)